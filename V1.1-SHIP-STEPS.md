# v1.1 — remaining ship steps (App Store Connect + RevenueCat)

Code is DONE and pushed (coin packs rebalanced to 200/$0.99, 500/$1.99,
800/$2.99 in commit 83355bf). These are the account-side steps left, in order.
New review screenshot: `shots/iap-shop-v2-1290x2796.png` (shows the new packs).

## 1. App Store Connect — product changes

**Reprice `tidal_coins_500`** (ID stays — it still grants 500 coins):
1. Monetization → In-App Purchases → **500 Coins** (`tidal_coins_500`).
2. Price Schedule → Edit / Plan a Price Change → **$1.99** → effective
   immediately → save.
3. Review Information → replace the screenshot with
   `shots/iap-shop-v2-1290x2796.png`.

**Create `tidal_coins_200`:**
1. In-App Purchases → **+** → Consumable → Reference Name `200 Coins` →
   Product ID exactly `tidal_coins_200` → Create.
2. Availability: all countries. Price: **$0.99**.
3. Add Localization (English U.S.): Display Name `200 Coins`, Description
   `Add 200 coins to your wallet.`
4. Upload the review screenshot → Save → status "Ready to Submit".

**Create `tidal_coins_800`:** same, with Reference Name `800 Coins`, Product ID
`tidal_coins_800`, price **$2.99**, Display Name `800 Coins`, Description
`Add 800 coins to your wallet.`

**Delete `tidal_coins_1500`:** open it → Delete (allowed pre-review; else
"Remove from Sale"). It must NOT be attached to the 1.1 version.

## 2. RevenueCat

1. Product catalog → Products → + New product → `tidal_coins_200`.
2. Again → `tidal_coins_800`.
3. Optionally delete `tidal_coins_1500` from the catalog.
4. No entitlement changes (coin packs stay unattached; only `tidal_premium`
   is attached to entitlement `premium`).

## 3. Rebuild + sandbox retest

1. Codemagic → Start new build → branch `main`, workflow `ios-tidal`
   (the pack change needs a new binary).
2. TestFlight: buy **200 Coins** in sandbox → wallet +200. NOTE: brand-new
   products can take a few hours to propagate to sandbox ("product not found"
   at first = wait); the repriced 500 pack works immediately.

## 4. Assemble the 1.1 version page

1. In-App Purchases and Subscriptions section: REMOVE `tidal_coins_1500` if
   listed; ADD `tidal_coins_200` + `tidal_coins_800` (they appear once Ready
   to Submit); keep `tidal_premium` + `tidal_coins_500`.
2. Swap the attached build for the new Codemagic build once processed.
3. Game Center section: leaderboard `tidal_high_scores` attached (done).
4. What's New:
   - Game Center leaderboard — compete for the top orbit
   - New Shop: Tidal Premium (2× coins) and coin packs
   - Music now sounds great on the phone speaker
   - Smoother continue flow with a confirm step
5. **App Privacy** (left sidebar): change from "no data collected" → add
   **Purchases → Purchase History, not linked to identity, not used for
   tracking** (RevenueCat processes purchase data).
6. Add for Review → Submit. App + 3 IAPs + leaderboard review together
   (~1–3 days).

## After approval
- IAPs + leaderboard go live with the version automatically.
- Then v1.2: AdMob rewarded-ad continue; EU trader status (DSA) to re-enable
  EU countries.
