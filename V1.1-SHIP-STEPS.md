# v1.1 — remaining ship steps (App Store Connect + RevenueCat)

Code is DONE and pushed. Coin packs 200/$0.99, 500/$1.99, 800/$2.99.
Review screenshot: `shots/shop-page.png` (or `shots/iap-shop-v2-1290x2796.png`).

## 1. App Store Connect — product changes ✅ DONE
- Repriced `tidal_coins_500` to $1.99; created `tidal_coins_200` ($0.99) and
  `tidal_coins_800` ($2.99); removed `tidal_coins_1500`. All 4 products made.

## 2. RevenueCat ✅ DONE
- `tidal_coins_200` + `tidal_coins_800` added to the catalog. Entitlement
  `premium` still attached to `tidal_premium` only (coin packs unattached).

## 3. Rebuild + sandbox retest ✅ DONE
- Codemagic build off `main` succeeded; sandbox-tested on device and purchases
  worked.

## 4. Assemble the 1.1 version page  ← YOU ARE HERE

1. In-App Purchases and Subscriptions section: REMOVE `tidal_coins_1500` if
   listed; ADD `tidal_coins_200` + `tidal_coins_800` (they appear once Ready
   to Submit); keep `tidal_premium` + `tidal_coins_500`.
2. Swap the attached build for the new Codemagic build once processed.
3. Game Center section: leaderboard `tidal_high_scores` attached (done).
4. What's New:
   - Game Center leaderboard — compete for the top orbit
   - New Shop: Tidal Premium (2× coins) and coin packs
   - Music now sounds great on the phone speaker
   - Smoother continue flow with a confirm step
5. **App Privacy** (left sidebar): change from "no data collected" → add
   **Purchases → Purchase History, not linked to identity, not used for
   tracking** (RevenueCat processes purchase data).
6. Add for Review → Submit. App + 3 IAPs + leaderboard review together
   (~1–3 days).

## After approval
- IAPs + leaderboard go live with the version automatically.
- Then v1.2: AdMob rewarded-ad continue; EU trader status (DSA) to re-enable
  EU countries.
